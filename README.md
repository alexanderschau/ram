# 🐏 Ram-Lang

An example script (`example.ram`):

```
📣 "hello world"
🕹️ res 📥 a b 📤 (➕ a b)
💾 result (➕ 2 4)
📣 result
```

## Tokens

- name (e.g. `📣`, `🕹️`)
- variable (e.g. `my_variable`)
- number (e.g. `123`)
- string (e.g. `"hello world"`)
- group (e.g. `(`, `)`)
